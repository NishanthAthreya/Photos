package View;

import java.io.File;

import javafx.scene.control.Label;
import javafx.scene.image.ImageView;
import javafx.scene.layout.HBox;

public class StockController {

	/*public void start()
	{
		for(int i = 0;i < pics.size();i++){
        	//System.out.println(pics.get(i));
        	ImageView imageView;
        	HBox hbox;
           // imageView = createImageView(new File(pics.get(i)));
        	hbox= createHbox(new File("./src/View/stockpic"+Integer.toString(i+1)+".jpg"), new Label(pics.get(i).getCaption()));
            tile.getChildren().addAll(hbox);
    	}
	}*/
}
